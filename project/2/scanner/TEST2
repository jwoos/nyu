/* just a legal-looking file to check that all the tokens work correctly */
int add1 (x)
{
  return x+1;
}

int main (int dummy)
{
  float x, y, z, w;
  int i, j, k;

  x = 0.1 + y * z / (w-2.3);

  y = -x; /* unary minus */

  while (x<y)
    {
      y=y+1;
      x=x+1;
    }

  return 73;
}
  

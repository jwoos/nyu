/* this one is syntactically correct */

int main(int dummy)
{
  int n, c, k;
 
  write "Enter an integer in decimal number system";

  read c; 
 
  while(c>=0)
  {
    k = n + c;
 
    if (k == 1)
      return 1 ;
    else
      return 0 ;
  }

  write "I am done.";

}

float a, b, c;
int y;

float test(float);

int main(int x) {
	int x,y:z; /* ERROR here: colon instead of a comma */
	int a, b, c;

	z = 1 + 2 - 3;
	p = -z * (x/345+y*1.0) + - 300;
	x = 1 / 2 * 3 / 4;

	/*
	while (p>=-(x+y)*3.45/6-z)
		z = z + 3;
		*/
	/*test(y);*/

	/*what(x);*/
}

float test(float x) {
	int c;
}
